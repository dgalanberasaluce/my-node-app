# my-node-app

Simple Express API scaffolded by Copilot.

Available scripts:

- `pnpm dev` — start with nodemon
- `pnpm start` — start with node

Endpoints:

- `GET /health` — return status
- `GET /api/hello` — greeting
- `POST /api/echo` — echoes JSON body
